<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cookfrombook">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CookFromBook">
        <activity android:name=".Like_activity"></activity>
        <activity android:name=".myUploadedrecipies" />
        <activity android:name=".PasswordActivity" />
        <activity android:name=".IntroductioryActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignupPage" />
        <activity android:name=".LoginPage" />
        <activity android:name=".RecipeActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".UploadRecipePage" />
    </application>

</manifest>